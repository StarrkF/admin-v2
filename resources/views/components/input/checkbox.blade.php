<div class="form-check">
    <div class="custom-control custom-checkbox">
        <input type="checkbox" value="{{ $value }}" class="form-check-input form-check-primary {{ $class }}"
            id="{{ $id }}" name="{{ $name }}" data-class="{{ $dataClass }}">
        <label class="form-check-label"
            for="{{ $id }}" role="button">{{ $label }}</label>
    </div>
</div>
